<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.maxime.android.myproject">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:theme="@style/Theme.Design.NoActionBar">
        <activity  android:name=".view.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.MainActivity2"></activity>
        <activity android:name=".view.MainActivity3"></activity>
        <activity android:name=".view.MainBisActivity"
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="Pokémon"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme"/>
        <activity android:name=".view.MainBisActivity2"
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="Ability"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme2"/>
    </application>

</manifest>